#ifndef __CONFIG_H__
#define __CONFIG_H__

void SetAspect(float aspect);
void SetDuration(float duration);

void SetDefaultSize(int size);
void SetDefaultFramerate(float fps);
void SetDefaultOversampling(int factor);

#endif
